<!DOCTYPE html>
<html>
<head>
    <title>VS CPU</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <header>
        <a class="retry" href="/cpu"><img src="/images/retry.png"><span>RETRY</span></a>
    </header>
    <main>
        <p class="<%= letter_color %>"><%= msg %></p>
        <form class="form_box" method="POST" action="/cpu/post">
            <input class="input_box" type="text" name="predict" maxlength="4" minlength="4" pattern="^[a-z]+$" autocomplete="off" spellcheck="false">
            <input class="btn_check" type="submit" value="CHECK">
        </form>
        <div class="list_box">
            <% for (var i = 0; i < content.length; i++) { %>
            <div>
                <p><%= content[i] %></p>
                <p><%= content2[i] %>H</p>
                <p><%= content3[i] %>B</p>
            </div>
            <% } %>
        </div>
    </main>
</body>
</html>
